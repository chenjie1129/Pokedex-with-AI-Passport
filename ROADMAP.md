# Roadmap：AI Passport 图鉴应用

更新日期：2026-09-10。评估基线：`d32aa17`、现有 README、领域代码和固件入口。

这是一份待执行的产品与开发计划。源码状态经过静态核对；本地 Host 测试此前因缺少 CMake 未能启动，固件编译、烧录和真机体验均未验证。下文所有验收指标都是目标。

**产品目标与首版边界**

把 TRAE AI Passport 做成一台愿意随身携带的掌上精灵图鉴：随时查看已发现的精灵，在不同地点遇见新内容，完成一次短捕捉后留下可长期保留的收藏。

按现有设计保留离线运行、Wi-Fi 环境识别、三键操作和本地存档。第一轮只有一只可捕捉精灵；正式探索试用时有三只。图鉴首页、列表、详情和收藏状态贯穿每轮。

当前 README 设定三只原创精灵，代码使用小火龙作为首个原型内容。T01 统一首测的角色表、命名与素材方向；第一轮沿用现有角色即可，正式资产制作放在实际体验反馈之后。

**当前起点：可复用什么，缺什么**

| 能力 | 当前可观察到的实现 | 排期影响 |
|---|---|---|
| 捕捉 | 有 C 语言时间窗口逻辑、固件动画和浏览器演示 | 复用交互骨架，统一浏览器与固件的节奏、超时和重试规则 |
| 图鉴 | 领域结构固定为小火龙；固件只有单页，地点与 `1 / 3` 文案写死 | 优先补独立入口、列表、详情和真实进度 |
| 按键 | 固件应用层仅处理 OK 点击 | 补上/下选择、确认和可见的返回入口 |
| 地点 | 有 Wi-Fi BSP、指纹与地点状态机；固件扫描页在 900 ms 后直接进入遭遇 | 接入真实扫描与持久化，才能验证走动价值 |
| 存档 | 领域层有版本、校验和事务回调；固件独立保存 `caught_004` 数字 | 将玩家资产统一交给领域服务，避免两套状态规则 |
| 长期使用 | 领域账本容量为 16，满后拒绝新增；缺少完整设备携带证据 | 账本容量、重启、耗电必须成为明确验收任务 |

代码依据：[固件入口](main/main.c)、[图鉴模型](components/city_domain/include/bestiary_service.h)、[图鉴结算](components/city_domain/src/bestiary_service.c)、[地点状态机](components/city_domain/src/location_mode.c)、[捕捉流程](components/city_domain/src/game_loop.c)、[现有计划](README.md)。

**优先级定义**

- P0：当前先做，建立可靠的一只精灵真机体验。
- P1：紧接着做，在正式的多地点、跨日产品试用前完成。
- P2：观察结果出来后再决定；不阻塞首轮验证。

这里是任务优先级，P1 中的地点、Wild Mode 和三只精灵仍属于 README 的首版 MVP 范围。任务顺序按体验依赖排列，负责人仅表示建议角色。

| ID | 优先级 | 任务与最小范围 | 依赖 | 完成标准 | 建议负责 |
|---|---|---|---|---|---|
| T01 | P0 | 定首版体验：启动页含“探索 / 图鉴”；确认三键规则、三种发现状态、角色表、捕捉总时长 | 无 | 一页流程和验收脚本；内部测试、地点测试、正式试用各有独立目标 | 产品 |
| T02 | P0 | 打通构建与真机：配置 CMake / ESP-IDF，执行已有测试，核对设备连接、屏幕、三键和恢复方式 | 无 | Host 测试、固件构建和设备运行分别留结果；得到可重复安装的基线包；保留原有分区与 Recovery 约定 | 固件 |
| T03 | P0 | 图鉴成为常驻入口：空列表、单条列表、详情和返回；上/下选择，OK 进入；每页显示操作提示 | T01、T02 | 开机即可浏览；未捕捉、捕捉失败、无 Wi-Fi 时均可进入；不必先完成捕捉 | 产品 / 固件 |
| T04 | P0 | 接通收藏状态：未知、已发现、已捕获；详情先含编号、名称、短描述和捕捉次数 | T01 | 遭遇未捕获也能留下已发现状态；进度来自存档；首轮支持一只，数据结构可扩展到三只 | 领域逻辑 |
| T05 | P0 | 统一存档和结算：固件调用领域服务，后台写 NVS，成功后刷新 UI；处理跨重启遭遇 ID 和 16 条账本上限 | T02、T04 | 成功、失败重试、重复输入、写入中断电、重启均不丢已提交收藏或重复加数；持续结算超过 16 次仍可用；压缩账本后旧事件也不会再次奖励 | 领域逻辑 / 固件 |
| T06 | P0 | 完成一只精灵闭环：遭遇、捕捉、失败反馈、收藏解锁、再次探索；统一 demo 与固件规则 | T03—T05 | 使用当前素材，在设备上连续走通；完整捕捉阶段的全部尝试合计控制在 10–20 秒；超时有明确结算；等待和写盘不冻结按键或画面 | 固件 / 交互 |
| T07 | P0 | 找 3–5 位未参与开发的人做第一次可用性观察，并修最主要的卡点 | T06 | 记录首次捕捉耗时、求助点、能否主动打开和退出图鉴；只决定交互修改，不推断留存 | 产品 / 测试 |
| T08 | P1 | 真地点接入：按需扫描、间隔复核、已知/候选/灰区状态、设备密钥与地点存档；拆开地点识别和奖励冷却 | T02、T05、T06 | 同地点 30 次扫描最多 1 次误判新地点；10 次跨建筑移动至少 9 次在到达并开始探索扫描后 60 秒内识别；五分钟奖励限制不阻止识别新地点；地点保存失败不显示确认成功 | 固件 / 领域逻辑 |
| T09 | P1 | 两地点两种发现：增加第二条图鉴内容和最小遭遇选择；首个新地点优先出现未收集精灵 | T04、T08 | 两个真实地点产生可感知的新内容；测试种子可复现；新地点的首次有效遭遇保证第二种发现；重复扫描同地点不持续刷出新物种 | 领域逻辑 / 产品 |
| T10 | P1 | 接通 Wild Mode：成功空扫/证据不足进入野外池；扫描故障保持状态；冷却和收藏共同可靠结算 | T05、T08 | 无 Wi-Fi 可完成专属野外遭遇；不增加地点数；30 分钟规则在重启和写盘失败后仍成立；灰区/扫描故障不发奖励 | 领域逻辑 / 固件 / 测试 |
| T11 | P1 | 携带体验：息屏、按需扫描、真实电量或明确不可用状态、恢复操作、低电量存档处理 | T02、T06、T08 | 在约定亮屏、扫描和捕捉次数下完成一次携带实测；恢复后可继续浏览和探索；低电量不破坏已保存资产 | 固件 / 测试 |
| T12 | P1 | 补齐三只精灵和图鉴动力：三种易分辨外观、短描述、不同捕捉节奏、动态收集进度和下一发现提示 | T07、T09、T10 | 三只可被实际收集；未知/已发现/已捕获视觉清楚；地点池与野外池有区分；提示能解释下一次探索有什么价值 | 内容 / 交互 / 领域逻辑 |
| T13 | P1 | 最小试用记录：启动、首次捕捉、新地点、浏览图鉴、错误计数；制定三天记录与结束访谈表 | T01；设备接入随 T06、T08推进 | 能算首次完成率和第二地点行为；记录固件版本；无原始 SSID/BSSID/精确位置；次日携带由实际记录确认 | 产品 / 测试 |
| T14 | P1 | 发布候选回归：存档升级、断电重启、账本持续使用、扫描失败、野外恢复、内存与镜像资源检查 | T08—T13 | Host 检查通过；固件构建通过；连续 30 分钟真机操作无崩溃；核心故障场景不丢收藏、不重复奖励；候选包可复现 | 测试 / 固件 |
| T15 | P1 | 10–15 人、至少三天正式试用，作出继续、调整或重新定位的判断 | T14；招募准备从 T07 开始 | 固定版本、预定统计口径、保留失败与缺失记录；按下方门槛出结论，反馈可关联到下一张任务卡 | 产品 |
| T16 | P2 | 提升图鉴吸引力：收藏精灵常驻展示、简短音效、详情翻页和动画打磨 | T07、T15 的具体反馈 | 只选择一个反馈最强的改进，对比是否增加主动查看图鉴或继续探索 | 交互 / 固件 |
| T17 | P2 | 内容与玩法扩展：更多精灵、陪伴成长、内容制作工具；再评估 BLE 合作等后续路线 | T15 达到继续门槛 | 每项绑定已观察到的需求；独立小实验验证 | 产品 / 内容 / 开发 |

T05 的实现可拆为接入存档、重启一致性、账本容量三张开发卡；T08 可拆为扫描接入、地点确认、两地点实测。拆卡后仍以可用体验作为迭代完成条件。

**当前实现进展**

- T03：已实现开机首页、探索/图鉴常驻入口、UP/DOWN 选择、OK 进入、空列表、单条列表、详情和返回；浏览器完成完整三键交互与 240×320 布局验收，生产固件已在真机启动到首页。
- T04：已实现 UNKNOWN/SEEN/CAPTURED 三态；首次遭遇在显示前事务性保存 SEEN，失败捕捉后仍可从首页查看；Host 与隔离真机 NVS 测试覆盖 SEEN 重启恢复及升级为 CAPTURED。
- 剩余人工验收：现场操作实体三键确认首页、列表和详情的手感与屏幕观感；该项不影响领域和持久化测试结论。

**建议迭代节奏**

以下按一名主要开发者、产品与测试协助、具备可连接设备来安排。采用 2–4 个工作日的小批次，属于排期建议；T02 先确认工具链与设备阻塞，再给具体日期。超出一个批次的工作继续拆分，预留原四周计划的最后一周做观察和修正。

| 轮次 | 本轮交付 | 对应任务 | 本轮要回答的问题 | 放行条件 |
|---|---|---|---|---|
| 第 0 轮：准备，0.5–1 天排查窗口 | 可重复构建、安装的设备基线；一页体验约定 | T01、T02 | 现在能否在实际硬件上快速改、装、看结果？ | 设备可运行并有恢复路径；受阻就明确阻塞，重新估时 |
| 第 1 轮：一只精灵的图鉴 | 开机可查图鉴，完成捕捉，断电后仍有记录 | T03—T07；T13 建表 | 按键、捕捉和收藏是否能被理解？ | 3–5 人观察完成；关键操作卡点已修；保存可靠 |
| 第 2 轮：带去第二个地点 | 真地点切换，发现第二种精灵，图鉴给出新目标 | T08、T09；同步测扫描耗电和接入记录 | 换地点是否真的带来新发现？ | 真实两地点链路与地点准确度通过；此轮先做受控体验观察 |
| 第 3 轮：可以带出去试用 | 三只精灵、Wild Mode、携带能力和记录完整 | T10—T14 | 连续玩、无信号和重启之后是否还能继续？ | 可靠性、携带和三只内容均过关；冻结候选版本 |
| 第 4 轮：三天产品实验 | 10–15 人真实使用，必要时只做一轮有针对性的修改 | T15 | 玩家会不会自发去第二个地点、次日再带？ | 根据预设规则决定下一步 |

第一轮如尚未接入真实地点，可保留明确标识的测试遭遇入口来验证图鉴和捕捉。第二轮受控测试由测试人员指定路线；第四轮对参与者不指定必须访问第二个地点，才能观察自发动机。

**需要优先定清的五条规则**

1. **图鉴的独立价值。** 首页始终有图鉴入口；发现但没抓到也留下一条可查看记录。第一轮详情以短文案和可读信息为主。
2. **一次捕捉的时长。** 现有领域逻辑每个计时回合 12 秒、最多三次机会，完整失败路径可能超过 README 的 20 秒目标。建议按整场遭遇分配 10–20 秒预算，机会共用预算，并与浏览器呈现保持一致。
3. **移动和冷却。** 现有地点状态机会在五分钟锁定期间提前返回。建议继续允许观察地点变化，将防刷奖励单独限制；跨建筑 60 秒从到达后主动开始探索扫描起计。后台自动探索可在后续证据支持时增加。
4. **第二地点的回报。** 首次到第二个确认地点，保证遇见尚未收集的第二种内容；后续再使用稳定的地点倾向。首测需要让“出门”与“图鉴变丰富”有清楚联系。
5. **携带与次日记录。** T11 初始建议场景为连续携带两小时、累计亮屏约 20 分钟、10 次探索扫描、10 次捕捉，作为待测目标；T02/T08 的功耗结果用于校准。先验证息屏和恢复，深睡唤醒必须以实机结果为准。无可靠跨重启日期时，三天记录与结束访谈确认次日携带，设备启动计数只作辅助。

**外部试用的验收与决策**

沿用 README 的三个目标，并增加图鉴行为观察。人数为 10–15 时按同一比例计算，预先固定分母为入组人数；失联和缺失单列，报告已确认比例和不确定性。数据不足时补充观察，不把缺失自动记为成功。

| 指标 | 口径与目标 |
|---|---|
| 首次捕捉 | 至少 80% 的参与者在三分钟内、无口头帮助完成首次捕捉 |
| 第二地点 | 至少 40% 的参与者在三天内自发前往第二个有意义的地点探索；结束访谈确认目的 |
| 次日携带 | 至少 30% 的参与者次日无开发者提醒再次携带或启动；将“携带”和“启动”分开记录，避免混称 |
| 图鉴价值 | 观察捕捉后是否主动打开图鉴、能否说明下一个想收集的对象；首轮用它定位问题，不另立未经校准的增长门槛 |

正式试用期间不发送探索或次日使用提醒；结束后再访谈。设备出错和无法继续使用计入体验结果，另标明技术原因，避免只统计顺利完成的人。

- 地点或存档验收未通过：先修技术问题，再开始动机实验。
- 样本或记录不足：补足证据，暂不作内容扩张决定。
- 三个产品指标和技术门槛均达标：进入内容扩展的小实验。
- 首次体验达标，而第二地点和次日指标都不超过 10%：重新评估出门探索定位，可研究以随身图鉴展示为核心的方向。
- 其余未达标组合：选择最明显的一处问题改一次，用同等规模复测；仍未达标就收缩或调整方向。

10–15 人测试用于早期方向筛选，不等同于稳定的市场或长期留存证明。

**每轮交付要求**

每轮留下一个版本明确的可玩包、一个短演示或设备记录、一份验收结果及下一轮最多三个关键问题。浏览器用于确认画面和交互，Host 测试确认领域规则，固件构建确认可编译，真机验收确认设备行为，分别报告。发生丢存档、假成功或无法继续操作的问题时，优先修复。

P2 的扩展应由试用结果触发。既有路线中的地图/GPS、账号云同步、交换/对战、排行榜、支付、语音召唤和 LLM 生成内容继续保持后置。静态图鉴说明足以支持当前收藏实验。

**硬件与设计依据**

官方基线确认了 240×320 屏幕、UP/DOWN/OK 三键、ESP32-C3 无 PSRAM，以及 8 MB Flash / 3 MB 应用分区等资源约束；低功耗、无线和电量效果仍需真机测量。它适合先采用小图、短文本和有限动画的图鉴交互。[FoloToy AI Passport 官方硬件能力说明](https://github.com/folotoy/ai-passport#hardware-capability-contract)

现有产品范围与成功门槛见 [README](README.md)，环境规则见 [W1 设计](docs/architecture/w1-environment-foundation.md)，首个捕捉样例见 [小火龙 MVP 设计](docs/architecture/charmander-capture-mvp.md)。本计划将这些设计重排为按体验交付的小批次，未改变应用代码。
